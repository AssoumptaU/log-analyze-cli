# This file ONLY handles Feature 3: Counting log levels

def increment_counter_by_one(current_total):

#This function takes a single counter number, adds 1 to it,
#   and returns the new updated total.

    new_total = current_total + 1
    return new_total